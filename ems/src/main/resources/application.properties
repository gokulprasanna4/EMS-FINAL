spring.application.name=Employee Management System
server.port=8090

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/ems_db_test
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Gokul@444}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.cors.allowed-origin=http://localhost:3000,http://localhost:5174,http://localhost:5173

# Security Configuration
# You can now put a PLAIN TEXT string here.
# IMPORTANT: Make sure this string is at least 32 characters long!
app.jwt.secret=${APP_JWT_SECRET:MySuperSecretKeyForEmployeeManagementSystem2025}
app.jwt.expiration=36000000

# Admin Defaults
app.admin.default-pass=${ADMIN_DEFAULT_PASS:admin123}